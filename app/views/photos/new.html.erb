<% content_for :title, Footprint32::APP_NAME %>
<div class="page-container">
  <div id="content-full">
    <div class="padding">
      
      <div class="title_text"><%= t('.title')%></div>

      <%= render 'form' %>

      <%= link_to t('.back'), user_photo_album_path(@user, @photo_album), :class => "general" %>

    </div>
  </div>
</div>
